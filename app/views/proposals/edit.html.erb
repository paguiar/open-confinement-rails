<h1><%= "#{I18n.t('actions.update')} #{@proposal.class.model_name.human}" %></h1>

<%= render 'form', proposal: @proposal %>

<%= link_to I18n.t('actions.show'), @proposal %>
|
<%= link_to I18n.t('actions.back'), proposals_path %>
