<h1><%= "#{I18n.t('actions.update')} #{@service.class.model_name.human}" %></h1>

<%= render 'form', service: @service %>

<%= link_to I18n.t('actions.show'), @service %>
|
<%= link_to I18n.t('actions.back'), services_path %>
